apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: gateway
spec:
  selector:
    app: auth-service  # Zorg dat de Deployment in de `auth` namespace deze label heeft
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
